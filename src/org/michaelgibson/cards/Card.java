package org.michaelgibson.cards;

/**
 * Interface that all cards implement.
 *
 * Currently it does nothing, but I wanted to have a base, rather than Cards just being Object.
 * So that if we add things to cards, it's obvious where to add those things, and so we can make Comparator&lt;Card&gt;
 * for example.
 */
public interface Card {
}
